# Large model binaries dropped next to index.html stay local (GitHub blocks
# files >100 MB). The small converted models in assets/ ARE committed so the
# app has real 3D models out of the box.
/*.glb
/*.gltf
/*.bin
!assets/
